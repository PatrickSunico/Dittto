= simple_form_for @post, html: {class: 'form-vertical'} do |f|
  = f.input :title, :required => false
  = f.input :thumbnail, as: :file, :required => false
  = f.input :description, :required => false
  = f.button :submit
